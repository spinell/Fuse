@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# TODO: Handle Static vs shared lib
# TODO: Handle Config (Debug/Release/...)
add_library(SDL3::SDL3-shared SHARED IMPORTED)
set_target_properties(SDL3::SDL3-shared
    PROPERTIES
        IMPORTED_LOCATION ${PACKAGE_PREFIX_DIR}/lib/SDL3.dll
        IMPORTED_IMPLIB   ${PACKAGE_PREFIX_DIR}/lib/SDL3.lib
)


include("${CMAKE_CURRENT_LIST_DIR}/FuseTargets.cmake")
